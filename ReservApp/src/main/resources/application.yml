spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:mysql-reservapp-amareie-ba4f.c.aivencloud.com}:${MYSQL_PORT:28634}/${MYSQL_DATABASE:defaultdb}
    username: ${MYSQL_USER:avnadmin}
    password: ${MYSQL_PASSWORD:AVNS_AIFGoTJwf_qeme21F8h}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
